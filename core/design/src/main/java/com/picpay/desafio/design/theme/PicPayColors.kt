package com.picpay.desafio.design.theme

import androidx.compose.ui.graphics.Color
import com.picpay.desafio.design.theme.values.Colors

data class PicPayColors(
    val primary: Color = Colors.Primary,
    val primaryDark: Color = Colors.PrimaryDark,
    val accent: Color = Colors.Accent,
    val primaryLight: Color = Colors.PrimaryLight,
    val detail: Color = Colors.Detail
)
